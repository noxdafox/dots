#!/bin/bash

PROJECTS=$HOME/development/

# add Python projects to PYTHONPATH
for package in $(find /home/noxdafox/development/ -mindepth 3 -maxdepth 4 \
                      -name "*__init__.py" | grep -Ev "doc|test")
do
    export PYTHONPATH=${PYTHONPATH}:${package%/*/*}
done
